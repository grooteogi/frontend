const user = {
  //   postHashtag: async (hashtag: string[]) => {
  //     const url = '/hashtag/post';
  //     const status = await postData(url, { hashtag });
  //     return status;
  //   },
};

export default user;
