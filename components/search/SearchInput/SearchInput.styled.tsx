import styled from '@emotion/styled';

const Styled = {
  container: styled.div`
    display: flex;
    flex-direction: row;
    justify-content: center;
    padding: 20px 0px;
  `,
};

export default Styled;
